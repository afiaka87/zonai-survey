add_executable(fidelity_tests test_policy.cpp test_trace.cpp test_aesthetics.cpp test_options.cpp)
target_compile_features(fidelity_tests PRIVATE cxx_std_20)
target_link_libraries(fidelity_tests PRIVATE doctest::doctest)
target_include_directories(fidelity_tests PRIVATE ../../src/surface ../../src/pure stubs)
add_test(NAME surface_scan COMMAND fidelity_tests)
foreach(profile IN ITEMS constrained tuning)
  add_executable(survey_${profile}_options ../test_main.cpp test_options.cpp)
  target_compile_features(survey_${profile}_options PRIVATE cxx_std_20)
  target_link_libraries(survey_${profile}_options PRIVATE doctest::doctest)
  target_include_directories(survey_${profile}_options PRIVATE ../../src/pure)
  target_compile_definitions(survey_${profile}_options PRIVATE SURVEY_CONSTRAINED=1)
  if(profile STREQUAL "tuning")
    target_compile_definitions(survey_${profile}_options PRIVATE SURVEY_TUNING=1)
  endif()
  add_test(NAME survey_${profile}_options COMMAND survey_${profile}_options)
endforeach()
find_program(UV_EXECUTABLE uv REQUIRED)
add_test(NAME shader_bindings COMMAND "${UV_EXECUTABLE}" run --no-project python "${CMAKE_CURRENT_SOURCE_DIR}/test_shader_bindings.py")
